Django==4.2.7
django-crispy-forms==2.1
crispy-bootstrap5==0.7
Pillow==10.1.0
reportlab==4.0.7
django-extensions==3.2.3
python-decouple==3.8
django-widget-tweaks==1.5.0
django-jet-reboot==1.3.7

# Reports App Dependencies
# PDF Generation (already included above)
# reportlab==4.0.7

# Excel Generation
openpyxl==3.1.2

# Caching for Performance
redis==5.0.1
django-redis==5.4.0

# Background Task Processing (for scheduled reports)
celery==5.3.4
django-celery-beat==2.5.0

# Additional Utilities
python-dateutil==2.8.2

# Development and Debugging (optional)
django-debug-toolbar==4.2.0

# PDF Email Attachments (choose one)
# For best quality HTML to PDF conversion (recommended but needs GTK on Windows):
# weasyprint>=60.0
# OR for simpler installation (limited CSS support):
xhtml2pdf>=0.2.11
